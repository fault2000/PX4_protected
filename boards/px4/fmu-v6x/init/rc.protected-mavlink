#!/bin/sh
# Manual TELEM UART bring-up; native USB NSH remains available.
echo "FMUv6X protected MAVLink bring-up"
ver all
work_queue status
# Permit transmission when an instance is started from NSH later.
mavlink boot_complete
